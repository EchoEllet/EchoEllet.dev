package dev.kernelcraft.site.constants

object Res {
    // TODO: Decide if this should be inline-svg in the HTML (which will be one widget instead of two images).
    const val GITHUB_MARK = "/github/github-mark.svg"
    const val GITHUB_MARK_WHITE = "/github/github-mark-white.svg"
}
